############################################################
# Pedal Construtivo — variáveis de ambiente (Vite)
#
# IMPORTANTE:
# - Nunca commitar `.env`, `.env.local` ou chaves reais no git.
# - Em produção (Vercel), configure pelas Environment Variables.
############################################################

# URL do projeto Supabase (apenas o domínio/origin, sem /rest/v1)
# Projeto oficial: golbuljslfenvghixhqt
VITE_SUPABASE_URL=https://golbuljslfenvghixhqt.supabase.co

# Anon/Public key do Supabase (Settings → Data API → Project API keys → anon/public)
VITE_SUPABASE_ANON_KEY=COLE_AQUI_A_ANON_PUBLIC_KEY
